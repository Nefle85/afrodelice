<nav class="navbar navbar-expand-lg navbar-dark custom-navbar">
  <div class="container-fluid">

    <!-- Restaurant logo and identity -->
    <a class="navbar-brand" href="{{ path('app_main') }}">
      <img src="{{ asset('divers/images/afrodelice.jpg') }}" alt="Logo du restaurant Afrodelice" class="logo">
      RESTO AFRO-EXOTIQUE
    </a>

    <!-- Toggler for mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navigation links-->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto">
        <li class="nav-item">
          <a class="nav-link home-link" href="{{ path('app_main') }}">
            <i class="fa fa-home"></i>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link menu-link" href="{{ path('app_menu') }}"> LA CARTE</a>
        </li>
        <li class="nav-item">
          <a class="nav-link actu-link" href="{{ path('app_post') }}">L'ACTU</a>
        </li>
        <li class="nav-item">
          <a class="nav-link presentation-link" href="{{ path('app_team') }}">QUI-SOMMES-NOUS ?</a>
        </li>
        <li class="nav-item">
          <a class="nav-link contact-link" href="{{ path('app_contact') }}">CONTACTEZ-NOUS</a>
        </li>

        <!-- Dashboard Item - Visible only to admins -->
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link admin-link" href="{{ path('admin') }}">ADMINISTRATION</a>
          </li>
        {% endif %}
      </ul>

      <!-- User Management -->
      <div class="header-user-management">
          {% if app.user %}
              <div>
                  {% if app.user.avatar is empty %}
                      <i class="fa-solid fa-circle-user"></i>
                  {% else %}
                      <img class="rounded-circle avatar-image" src="{{ asset('divers/avatars/' ~ app.user.avatar) }}" alt="User Avatar">
                  {% endif %}
                  <span class="zxfirstname">{{ app.user.firstname|capitalize }}</span>
                  <a class="loggouts" href="{{ path('app_logout') }}">
                      <i class="fa-solid fa-power-off"></i>
                  </a>
              </div>
          {% else %}
              <a class="login" href="{{ path('app_login') }}">
                  <i class="fa-regular fa-circle-user"></i>&ensp;Se connecter
              </a>
          {% endif %}
      </div>

    </div>
  </div>
</nav>